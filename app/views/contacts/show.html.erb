<h1>Contact Informations</h1>
<div>Email: <%= @contact.email %></div>
<table>
  <tr>
    <th>URL</th>
    <th>Moment</th>
    <th></th>
    <th></th>
    <th></th>
  </tr> 
<% @accesses.each do |access| %>
  <tr>
    <td><%= access.url %></td>
    <td><%= access.created_at %></td>
  </tr>
<% end %>